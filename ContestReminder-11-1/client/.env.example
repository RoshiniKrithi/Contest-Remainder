# Backend API URL — set this to your Render backend URL in production
# Leave empty in development (Vite proxy handles /api/* → localhost:5000)
VITE_API_URL=https://contestreminder-backend.onrender.com
